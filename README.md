# alarm-microservice
The project includes a microservice written in Golang that handles the registration, authentication of new users and stores active user data in a database. It is meant to work together with Home Assistant as a custom alarm extension.
